package identifiers;

public class rules {
	/* identifier is a any java menthode name,variable name,class name is called idetifier.
	  1.it allows is a-z,A-Z,0 TO 9 ,$,_,but should not start with number and doesnt allows specail characters.
	  2.identifiers should  ot allows duplicates values.
	  3.indefirer shouc not allows keywords.
	  4.indentifiers are case senstive.
	  5.it is possible to take predefined class-name and interface names as a
	   identifiers but is not recommbed.
	   
	   		int String=100;
		System.out.println(String);
		
		int serailizable=44;
		System.out.println(serailizable);

	  */
	
	public static void main(String[] args) {
		int String=100;
		System.out.println(String);
		
		int serailizable=44;
		System.out.println(serailizable);
	}

}
